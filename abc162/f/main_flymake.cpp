#include<bits/stdc++.h>
using namespace std;
#define ll long long int

int main() {
    ll N, ans=0, odd=0, even=0;    
    cin >> N;
    vector<ll> a(N);

    for(int i=0; i<N; i++){
	cin >> a[i];
	if(i%2!=0)
	    odd += a[i];
	else
	    even += a[i];
    }

    if(N)




    


    cout << ans << endl;
} 


