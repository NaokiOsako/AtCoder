#include<bits/stdc++.h>
using namespace std;
#define ll long long int

int main() {
    int N, ans=0, a, b, c;
    cin >> a >>b >>c;
    swap(a, b);
    swap(a, c);
    cout << a <<" "<< b <<" "<< c << endl;
} 


